package entity

class Place {
    def name
    def vicinity
    def distance

    Place(name, vicinity, distance) {
        this.name = name
        this.vicinity = vicinity
        this.distance = distance
    }
}
