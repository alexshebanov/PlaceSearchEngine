package resultHandling


class TemporaryResult {
    final String status
    final def placesList

    TemporaryResult(String status, placesList) {
        this.status = status
        this.placesList = placesList
    }
}
